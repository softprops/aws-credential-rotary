import * as assert from "assert";

// https://github.com/dwyl/aws-sdk-mock#using-typescript

describe("main", () => {
  describe("rotate", () => {
    it("works", () => {
      assert.equal(1, 1);
    });
  });
});
